#pragma once

namespace eglyf::res {

namespace detail {
inline constexpr char const raw[] = @chunks@;
}

inline constexpr std::string_view const vtp{detail::raw,
                                            sizeof(detail::raw) - 1};

} // namespace eglyf::res
