#pragma once

namespace eglyf {

class GlyphInsertion {
  GlyphInsertion() = delete;

public:
  enum class Pos {
    TopStart,
    BottomStart,
    TopEnd,
    BottomEnd,
  };

  // static
};

} // namespace eglyf
